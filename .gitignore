# Claude Code per-machine local state (auto-generated, contains user-specific
# permissions / approved commands — never commit).
.claude/settings.local.json

# Root-level MCP config (auto-generated by various tools; keep machine-specific).
# Note: .vscode/mcp.json is intentionally tracked — it's the project-shared one.
/.mcp.json

# OS / editor noise
.DS_Store
Thumbs.db
*.swp
*.swo

# Logs / dumps
*.log
npm-debug.log*

# Node — currently no build step but harmless to pre-empt
node_modules/
